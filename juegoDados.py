#Dos amigos quieren jugar al backgammon, pero han perdido los dados.
#Crea un programa para reemplazar los dados. Cuando se ejecuta el programa, debe tirar los dados y mostrar el resultado de cada dado.
#Pista
#Utilice la función random.randint () para generar valores aleatorios en el rango de 1 a 6 para cada dado.
#Verá una línea random.seed (int (input ())) en el código de muestra. Inicializa el generador de números pseudoaleatorios y, 
#en este caso, asegura la funcionalidad de los casos de prueba.

import random
random.seed(int(input())) #please don't touch this lane

#generate the random values for every dice
dice1 = 
dice2 = 

print(dice1)
print(dice2)