list = [2, 3, 4, 5, 6, 7]

for x in list:
          if(x%2==1 and x>4):
                    print(x)
                    break #Rompera el ciclo for imprimira el numero de posiciones que hay en la lista
#Por lo general, usamos el bucle for cuando el número de iteraciones es fijo. Por ejemplo, iterar sobre una lista fija de artículos 
#en una lista de compras.
#El ciclo while es útil en los casos en que no se conoce el número de iteraciones y depende de algunos cálculos y condiciones en el bloque de 
# código del ciclo.
#Por ejemplo, finalizar el ciclo cuando el usuario ingresa una entrada específica en un programa de calculadora.